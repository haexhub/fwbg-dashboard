<!-- spaex-composed:source_hash="9e378926812840ba0c71ae135dce132e42749b3078b77aa30948dc19eb9a3747" build_input_hash="af2980b398313f9fd776b0fbdde915f784cc47a246cc19e3c830d50e891cc668" version="1" -->
# spaex Behavior Harness

_This file is generated by `spaex install`. Do not edit by hand._
_Change fragments in `.spaex/constitution.d/` and re-run install._

## MUST

- For any existing Git repository, all changes — source code, tests, documentation, configuration, and generated files that are part of the change — must be authored in a dedicated repository-local worktree on a topic branch (a directory registered for the repository by `git worktree list`, not a copied directory or unrelated clone). _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Test code must be maintained in files separate from production code in every language, for unit, integration, contract, and end-to-end tests; a language-required test-module declaration or registration may remain in the production file, but test bodies and test-only helpers must live in dedicated test files, following the repository's convention for test-file placement and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat 500 lines of hand-maintained source as the upper boundary for a single file: a file clearly over that boundary must be split, or have an explicit, documented reason to remain large together with a concrete plan for a later split. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Apply full rigor rather than a lazy shortcut to understanding the problem, input validation at trust boundaries, error handling that prevents data loss, security, accessibility, the calibration real hardware needs, and anything explicitly requested. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Leave one runnable check behind for non-trivial logic — the smallest thing that fails if the logic breaks — written with whatever test framework or tooling the project has already adopted; a project with no such framework yet gets a framework-free assert-based demo or one small test file instead, and trivial one-liners need no test. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## MUST_NOT

- Do not assume that a platform-specific global or API exists everywhere; browser, Node.js, and serverless environments have different APIs and failure modes. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not catch an error merely to log and rethrow it unchanged; that usually duplicates logs and loses ownership of the failure decision. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not introduce a barrel export or a new abstraction only to shorten an import path. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not leave promises unhandled. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not replace repository tooling with a personal global installation. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not use `||` when a valid value such as `0`, `false`, or `""` must be preserved, and do not hide a required value behind optional chaining without deciding how absence is handled. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not use mutation or shared mutable module state as an accidental communication channel. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not assume that a test file is discovered unless the runner configuration confirms it. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Do not leave floating promises in tests, and do not let a test pass before the asynchronous work it is meant to verify has completed. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Do not mock the unit's own logic so thoroughly that the test only verifies the mock wiring. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Do not turn every test into a slow end-to-end test. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Do not author changes in the primary checkout; reserve it for inspection and worktree management (read-only inspection of any checkout is fine). _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not create folders only for superficial symmetry, such as putting every small class, interface, or helper in its own folder. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not enforce a universal maximum for functions, files, or pull requests; domain complexity, cohesion, control flow, and the number of reasons to change are more meaningful than a line count. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not turn a green-looking diff into an unverified claim. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not use comments to narrate code that could be made clear through structure and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- LLM agents must not leave references to themselves, their model, runtime, role, prompts, or participation in project artifacts — including commit messages, commit trailers such as `Co-authored-by`, source and documentation comments, changelogs, generated content, metadata, and other repository content. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Never add artificial wrappers, blank lines, or extra files only to satisfy a line-count threshold. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not introduce an abstraction merely because two things look similar, or a speculative abstraction solely because a change might be reusable; first establish that they share the same behavior, ownership, change reason, and invariants — a small amount of local duplication is preferable to a premature generic abstraction that couples unrelated callers. A concrete, domain-owned abstraction that improves cohesion, removes real duplication, or makes a branch independently testable is not premature, and may be used even when the task did not name the extraction, kept as small as possible. _[from `com.github.haexmas.atoms.general-coding/general-coding`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not add boilerplate nobody asked for. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD

- Follow the existing runtime and module-system conventions, since browser, Node.js, and serverless environments have different APIs and failure modes. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Keep asynchronous control flow explicit: `await` promises whose result or failure matters, handle errors at the boundary that can recover or report them, and use `Promise.all` only when operations are independent and fail-fast behavior is acceptable. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Keep modules and exports focused: prefer named exports for reusable library code, and avoid circular dependencies. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Keep untyped escape hatches local, and explain why they are necessary. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Prefer `const` and narrow local mutation, using `let` when reassignment makes the state transition clearer. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Prefer immutable transformations at shared boundaries, but allow simple local mutation when it is clearer and measurably avoids needless allocation — readability and correct ownership of state matter more than a blanket immutability rule. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Preserve error causes when translating errors, adding useful context at the boundary. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Treat TypeScript's `strict` mode as the baseline for new or actively maintained TypeScript code. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Use `===`/`!==`, `??`, and `?.` deliberately. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Validate data at trust boundaries — HTTP responses, request bodies, files, environment variables, message queues, and browser storage — by parsing or validating it once and passing the resulting domain type inward, since a TypeScript type assertion has no runtime effect and does not make the data safe. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Use the project's configured formatter, linter, compiler, environment, and test runner; at minimum, TypeScript changes should pass `tsc --noEmit` (or the project's equivalent), linting, formatting checks, and the relevant tests, and a change should include relevant tests and pass the project's normal test command. Before handoff, run the repository's formatter, static checks, and relevant tests, and report what was run and any checks that were unavailable or skipped. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`, `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Choose the narrowest test level that proves the behavior: unit tests for pure logic, integration tests for module boundaries and persistence, and end-to-end tests for critical user journeys. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- For TypeScript, keep type-level tests separate from runtime tests when the project needs to verify inference, assignability, or declaration output, since runtime tests cannot prove a compile-time contract and the compiler cannot prove runtime validation of external data. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Keep mocks narrow and behavior-oriented, mocking an external boundary only when isolation or determinism requires it. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Keep tests deterministic and isolated: avoid real network calls, shared mutable state, random values without a controlled seed, and arbitrary timer sleeps, and control time, randomness, environment, and external clients at explicit boundaries. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Prefer a small number of clear assertions per scenario over a large fixture that hides what the test is proving. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Test both successful and failure behavior, including malformed input, missing values, rejected promises, cancellation or timeout behavior, and authorization or persistence failures where they matter. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Test names should describe the behavior and relevant condition, not the implementation method name alone. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Use `Promise.all` or `Promise.allSettled` when a test intentionally exercises concurrent work and its failure semantics, and await every promise that affects the assertion. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Test observable behavior and public contracts, not the exact arrangement of private functions or implementation details, so that a refactor preserving the contract does not require rewriting the test suite, and so a test is useful only when it can fail for a real regression. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- A file should have one primary reason to change; split it when it has unrelated responsibilities, independent public concepts, different change owners, or tests that are difficult to isolate, but do not split solely to make files shorter. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Add or update tests with behavior changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before the first edit, verify the repository root, the active branch, and the registered worktrees, and create a suitable worktree first if none exists. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Comments should explain why a non-obvious decision exists, what invariant must be preserved, or which external constraint applies, and should be updated when the behavior changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Give each function one coherent responsibility at one level of abstraction; extract a helper when it gives a meaningful name to a concept, makes a branch independently testable, or removes repeated logic, not merely because the function crossed an arbitrary line count. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep dependencies directed and understandable: avoid circular imports, hidden global state, and a domain layer that knows about infrastructure details, and put I/O, framework adapters, and process wiring at the edges when the architecture calls for such a separation. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each change self-contained and easy to review: production code, relevant tests, documentation, and configuration belong together when they describe the same behavior, and independent refactors or unrelated cleanups should be split into separate changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each task isolated in its own worktree and topic branch so that changes can be reviewed, tested, committed, and removed independently. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep public module interfaces narrow and intentional, hiding implementation details behind the smallest stable API the callers need, and avoid re-exporting everything or creating pass-through modules that add no boundary. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep related tests close to the behavior they verify according to the language and repository convention, since a module split is not complete if its tests, documentation, fixtures, and call sites are left misleadingly behind. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep side effects at visible boundaries, so pure calculations and decisions are easy to test without a database, network, filesystem, clock, or global mutable state. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Name code after its domain meaning and behavior, rather than relying on a comment to explain an otherwise opaque `data`, `helper`, or `process` function. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer a change that clearly improves maintainability and correctness over theoretical perfection; when a design is uncertain, make the smallest reversible change that provides evidence about the next decision. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer early returns or named helpers when they make the main path easier to follow, reducing nesting without scattering one simple operation across many microscopic functions. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Refactor repeated behavior when the shared rule is real and the abstraction makes both callers clearer, keeping the abstraction close to the domain that owns it and avoiding a generic utility module becoming a dumping ground. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Separate broad formatting or mechanical refactors from behavior changes when possible, since mixed diffs make review, debugging, rollback, and blame less reliable. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat a change of about 100 net lines as a useful review-size warning, and a change approaching 1000 lines as a strong prompt to split the work or agree on the larger change in advance, except for identified generated code, mechanical renames, and required migrations. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Use smaller soft thresholds — a function or method beyond roughly 40–60 lines, or nesting deeper than roughly three levels — as a prompt for deliberate review, not as an automatic failure. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- When a repository already has a documented structure, naming scheme, or size convention, follow it unless the task explicitly changes that convention — these defaults are not a reason to reformat or reorganize an entire codebase. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before writing any code, stop at the first rung of the ladder that holds — confirm the code needs to be built at all, reuse an existing helper or pattern already in the codebase, use the standard library, use a native platform feature, use an already-installed dependency, write it as one line, or only then write the minimum code that works — but climb the ladder only after understanding the problem and tracing the real flow, not instead of it. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- For a bug fix, address the root cause rather than the symptom: grep every caller of the function you touch and fix the shared function once, rather than patching only the path the ticket names. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Mark a deliberate simplification that cuts a real corner with a known ceiling (global lock, O(n²) scan, naive heuristic) with a `ponytail:` comment naming the ceiling and upgrade path. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Prefer deletion over addition, boring over clever, and the fewest files possible. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Prefer the shortest working diff, but only once the problem is understood — the smallest change in the wrong place isn't lazy, it's a second bug. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Question complex requests: confirm whether something is actually needed or a simpler alternative already covers it. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- When two standard-library approaches are the same size, pick the edge-case-correct option — lazy means less code, not the flimsier algorithm. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD_NOT

- Avoid `any`; use a precise type, a generic, or `unknown` followed by explicit narrowing instead. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Avoid adding a new dependency when it can be avoided. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## MAY

- Existing projects may opt out of individual strict checks during migration, but should make that choice explicit. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Generated files, vendored code, snapshots, and other files not normally edited by hand may exceed the 500-line boundary, but should be excluded from this assessment. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
