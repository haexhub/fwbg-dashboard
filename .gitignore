node_modules
.nuxt
.output
*.log
data/
.DS_Store

# Test artifacts
playwright-report/
test-results/
playwright/.cache/

# Local config
.nuxtrc
*.local.*

# Lock files (use bun.lock)
pnpm-lock.yaml
package-lock.json
yarn.lock
.env

# graphify (regenerated by `graphify update .` or the post-commit hook installed via `graphify hook install`)
graphify-out/
